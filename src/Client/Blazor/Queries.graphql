query me {
  me {
    ...user
  }
}

fragment user on Person {
  id
  name
  email
}

query people {
  people(where: { email_not: "foo@bar.com" }) {
    nodes {
      ...person
    }
  }
}

fragment person on Person {
  id
  name
  email
  lastSeen
}
